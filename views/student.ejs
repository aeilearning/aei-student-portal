<!doctype html>
<html>
<head>
  <meta charset="utf-8" />
  <title>AEI Student</title>
  <link rel="stylesheet" href="/styles.css" />
</head>
<body class="container">

  <div class="topbar">
    <div>
      <h1>Student Dashboard</h1>
      <div class="muted">Logged in as <b><%= user.email %></b></div>
    </div>
    <a class="link" href="/logout">Logout</a>
  </div>

  <% if (message) { %>
    <div class="alert"><%= message %></div>
  <% } %>

  <div class="card">
    <h2>Profile</h2>
    <div class="grid2">
      <div class="stack">
        <label>Name</label>
        <div class="muted"><%= (student.first_name || '') %> <%= (student.last_name || '') %></div>
      </div>

      <div class="stack">
        <label>Phone</label>
        <div class="muted"><%= student.phone || '' %></div>
      </div>

      <div class="stack">
        <label>Employer</label>
        <div class="muted"><%= student.employer_name || '' %></div>
      </div>

      <div class="stack">
        <label>Level</label>
        <div class="muted"><%= student.level %></div>
      </div>

      <div class="stack">
        <label>Status</label>
        <div class="muted"><%= student.status || '' %></div>
      </div>
    </div>

    <div class="muted small" style="margin-top:10px;">
      Student self-service editing and uploads are coming next. For now, admin manages changes and document uploads.
    </div>
  </div>

  <div class="card">
    <h2>Documents</h2>
    <div class="muted">
      Student document vault isn’t wired yet for student accounts. Admin can upload documents on your behalf.
    </div>

    <% if (documents && documents.length) { %>
      <div style="margin-top:10px;" class="muted small">
        (You have <%= documents.length %> document(s) queued to display, but student download routes aren’t enabled yet.)
      </div>
    <% } else { %>
      <div class="muted small" style="margin-top:10px;">No documents available yet.</div>
    <% } %>
  </div>

</body>
</html>
