<!doctype html>
<html>
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>AEI Employer Portal</title>
  <link rel="stylesheet" href="/styles.css" />
</head>
<body class="container">

  <div class="topbar">
    <div>
      <h1>Employer Portal</h1>
      <div class="muted">Signed in as <b><%= user.email %></b></div>
    </div>
    <a class="link" href="/logout">Logout</a>
  </div>

  <div class="card">
    <h2>Account Information</h2>

    <form method="POST" action="/employer/update" class="grid2">
      <div class="stack">
        <label>Email (read-only)</label>
        <input value="<%= user.email %>" disabled />
      </div>

      <div class="stack">
        <label>Company Name</label>
        <input name="company_name" value="<%= employer.company_name || '' %>" placeholder="Company name" />
      </div>

      <div class="stack">
        <label>Primary Contact</label>
        <input name="contact_name" value="<%= employer.contact_name || '' %>" placeholder="Contact person" />
      </div>

      <div class="stack">
        <label>Phone</label>
        <input name="phone" value="<%= employer.phone || '' %>" placeholder="Phone number" />
      </div>

      <div>
        <button class="btn">Save Changes</button>
      </div>
    </form>
  </div>

  <div class="card">
    <h2>Coming Next</h2>
    <ul class="small muted">
      <li>View apprentices associated with your company</li>
      <li>Verify employment information</li>
      <li>Upload forms and documentation</li>
      <li>Administrative coordination with AEI</li>
    </ul>
  </div>

</body>
</html>
