<!doctype html>
<html>
<head>
  <meta charset="utf-8" />
  <title>AEI Employer</title>
  <link rel="stylesheet" href="/styles.css" />
</head>
<body class="container">

  <div class="topbar">
    <div>
      <h1>Employer Dashboard</h1>
      <div class="muted">Logged in as <b><%= user.email %></b></div>
    </div>
    <a class="link" href="/logout">Logout</a>
  </div>

  <% if (message) { %>
    <div class="alert"><%= message %></div>
  <% } %>

  <div class="card">
    <h2>Company Profile</h2>
    <div class="grid2">
      <div class="stack">
        <label>Company Name</label>
        <div class="muted"><%= employer.company_name || '' %></div>
      </div>

      <div class="stack">
        <label>Contact Name</label>
        <div class="muted"><%= employer.contact_name || '' %></div>
      </div>

      <div class="stack">
        <label>Phone</label>
        <div class="muted"><%= employer.phone || '' %></div>
      </div>
    </div>

    <div class="muted small" style="margin-top:10px;">
      Employer self-service editing is coming next. For now, admin manages profile updates.
    </div>
  </div>

  <div class="card">
    <h2>Documents</h2>
    <div class="muted">
      Document vault for employers isn’t wired yet in <code>server.js</code>.
      Uploads/downloads are currently admin-only for student files.
    </div>

    <% if (documents && documents.length) { %>
      <div style="margin-top:10px;" class="muted small">
        (You have <%= documents.length %> document(s) queued to display, but download routes aren’t enabled yet.)
      </div>
    <% } %>
  </div>

</body>
</html>
