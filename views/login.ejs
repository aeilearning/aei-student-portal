<!doctype html>
<html>
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>AEI Portal Login</title>
  <link rel="stylesheet" href="/styles.css" />
</head>
<body class="container">

  <div class="card">
    <div class="header">
      <h1>AEI Learning Portal</h1>
      <p class="muted">Sign in with your email and password.</p>
    </div>

    <% if (message) { %>
      <div class="alert"><%= message %></div>
    <% } %>

    <form method="POST" action="/login" class="stack">
      <div class="stack">
        <label for="email">Email</label>
        <input id="email" name="email" type="email" autocomplete="email" required />
      </div>

      <div class="stack">
        <label for="password">Password</label>
        <input id="password" name="password" type="password" autocomplete="current-password" required />
      </div>

      <button type="submit" class="btn">Login</button>
    </form>

    <div class="divider"></div>

    <div class="row">
      <a class="link" href="/reset-password">Forgot password?</a>
      <a class="link" href="/register">Request access</a>
    </div>

    <p class="muted small mt">
      If you need access or a password reset, submit a request above and AEI will follow up.
    </p>
  </div>

</body>
</html>
